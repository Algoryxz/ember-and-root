'use client';

import React from 'react';
import { HearthView } from '../features/hearth';

/**
 * Root Home Page (/) — Primary Daily Action Space (Hearth)
 * Owned by: Deeptiman (Experience / Frontend Lead)
 * 
 * In Ember & Root, today's journey begins at the Hearth.
 */
export default function HomePage() {
  return <HearthView />;
}
