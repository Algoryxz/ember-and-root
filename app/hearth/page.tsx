'use client';

import React from 'react';
import { HearthView } from '../../features/hearth';

/**
 * Hearth Page — Primary Daily Action Space
 * Owned by: Deeptiman (Experience / Frontend Lead)
 * 
 * Mounts the authoritative Hearth experience into Next.js application structure.
 */
export default function HearthPage() {
  return <HearthView />;
}
